/* The Document Object Model (DOM) connects web pages to scripts or programming languages by representing the structure of a document—such as the HTML representing a web page—in memory */


document.getElementById('welcome')



// Now in the storing the value can easly make the manipulatio easy...

document.getElementsByClassName('JiBaataieNa')

// We can also find Elemnet through the attribute class,id etc

document.getElementById('welcome').getAttribute('id')

// We can also set the attribute in the null aur class 
document.getElementById('welcome').setAttribute('class','ji' )//It shows Undefind and Overwrite and change id or class 

//we can also access it though the varible name 
const value = document.getElementById('welcome')
// we can also preform style through the javscript
//For backgroundcolor
count.style.backgroundColor='red'
//For padding
count.style.padding='15px'

// We can also get Element by ClassName 
document.getElementsByClassName('JiBaataieNa')

/***************************** Query Selector **********************************/
document.querySelector('h1')
document.querySelector('h2')
document.querySelector('h3')
document.querySelector('h4')

// doc